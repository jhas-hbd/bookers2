<h1>Book detail</h1>
<div>
  <p><%= @book.title %></p>
  <p><%= @book.body %></p>
</div>

<h1>New Book</h1>
  <%= form_with model: @post do |f| %>
    <h4>Title</h4>
      <%= f.text_field :title %>
    <h4>Opinion</h4>
      <%= f.text_area :body %>
    <%= f.submit 'Create Book' %>
  <% end %>