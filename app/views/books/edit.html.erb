<h1>Editing Book</h1>
<%= form_with model: @edit do |f| %>
  <label for="inputTitle">Title</label>
  <%= f.text_field :title %>
  <label for="inputBody">Opinion</label>
  <%= f.text_area :body %>
  <%= f.submit "Update Book" %>
<% end %>